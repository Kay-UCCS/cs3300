language: ruby
cache: bundler
bundler_args: --without production

rvm:
 - 2.7.0
 
before_install:
 - gem install bundler:2.2.11

script:
 - bundle exec rspec
 
deploy:
 provider: heroku
 api_key: d79a41d5-1b8f-404f-8553-5f0d9e00dabf
 app: fast-tundra-11242
 run: "rake db:migrate"
 on:
  all_branches: true